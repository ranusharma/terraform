pipeline {
  agent any

  environment {
    OKTA_API_TOKEN = credentials('OKTA_API_TOKEN')
  }

  stages {
    stage('Checkout') {
      steps {
        git url: 'https://github.com/ranusharma/terraform.git', branch: 'main'
      }
    }

    stage('Terraform Init') {
      steps {
        sh 'terraform init'
      }
    }

    stage('Terraform fmt-chek') {
    steps {
        sh 'terraform fmt -check'
      }
    }
    stage('Terraform Plan') {
      steps {
        sh 'terraform plan -var="api_token=$OKTA_API_TOKEN"'
      }
    }

    stage('Terraform Apply') {
      steps {
        input message: 'Apply changes?'
        sh 'terraform apply -auto-approve -var="api_token=$OKTA_API_TOKEN"'
      }
    }
  }
}
